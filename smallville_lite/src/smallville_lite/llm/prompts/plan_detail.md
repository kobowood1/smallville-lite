---
id: plan_detail
version: 1
---
Today is $date. Nearby parts of $first's schedule:
$context

From $start to $end ($minutes minutes) $first is $activity at $place.
Objects available at $place: $objects

List the concrete steps $first takes during this time, in 5, 10 or 15 minute increments, as the activity would really unfold (for example: gather materials, start, take a short break, tidy up). The minutes must add up to exactly $minutes. For each step give the activity (phrased to complete "$first is ..."), the object used (or "none"), and the object's state while in use (for example "brewing coffee"; empty if no object).
